package com.campusdual.bfp.model.dao;

public interface CandidateDao extends UserDao {


    // List<Candidate> findBySkill(String skill);
    // List<Candidate> findByExperienceGreaterThan(int years);

    // Note: The actual implementation of these methods would depend on the underlying database and ORM framework used.
}
